[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fwdutil"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "yamllib^=0.0.1",
    "sqlalchemy^=2.0.36",
    "requests^=2.32.3",
]

[tool.setuptools]
package-dir = {"" = "src"}
